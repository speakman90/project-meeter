{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

    {{ form_start(registrationForm, {'attr': {'class': 'flex flex-col items-center', 'style': 'margin: 30px 0px'}}) }}

    <div class="w-full p-20 flex flex-row justify-around">
        <div class="flex flex-col justify-between">
            {{ form_row(registrationForm.email) }}

            {{ form_row(registrationForm.userName, {
                label: 'Ton prénom'
            }) }}

            {{ form_label(registrationForm.birthDate) }}
            {{ form_widget(registrationForm.birthDate) }}
            {{ form_errors(registrationForm.birthDate) }}

        </div>
        <div class="flex flex-col justify-between">
            {{ form_row(registrationForm.gender) }}

            {{ form_row(registrationForm.profilPhotos) }}

            {{ form_row(registrationForm.biography) }}
        </div>
        <div class="flex flex-col justify-center">
            {{ form_row(registrationForm.plainPassword, {
                label: 'Mot de passe'
            }) }}
            <div class="flex items-center justify-around" style="margin: 30px">
                {{ form_widget(registrationForm.agreeTerms) }}
                {{ form_label(registrationForm.agreeTerms) }}
                {{ form_errors(registrationForm.agreeTerms) }}
            </div>
        </div>
    </div>
    <div class="flex" style="margin: 1rem">
        <button type="submit" class="relative inline-flex items-center justify-center p-0.5 mb-2 mr-2 overflow-hidden text-sm font-medium text-gray-900 rounded-lg group bg-gradient-to-br from-purple-500 to-pink-500 group-hover:from-purple-500 group-hover:to-pink-500 hover:text-white dark:text-white focus:ring-4 focus:outline-none focus:ring-purple-200 dark:focus:ring-purple-800">
            <span class="relative px-5 py-2.5 transition-all ease-in duration-75 bg-white dark:bg-gray-900 rounded-md group-hover:bg-opacity-0">
                S'inscrire
            </span>
        </button>
    </div>

    {{ form_end(registrationForm) }}

{% endblock %}
